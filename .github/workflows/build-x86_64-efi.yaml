name: 编译ImmortalWrt x86_64 EFI固件（中文+3GB overlay+修复swap占用）

on:
  workflow_dispatch:  # 仅手动触发

jobs:
  build-firmware:
    runs-on: ubuntu-22.04
    timeout-minutes: 180
    env:
      OVERLAY_SIZE: 3072M
      TARGET_ARCH: x86/64
      BRANCH: openwrt-23.05

    steps:
      - name: 🔧 系统初始化 & 深度清理空间（修复swap占用+抑制apt警告）
        run: |
          # ========== 1. 优化apt命令，抑制CLI不稳定警告 ==========
          sudo apt update -y -qq  # -qq：静默模式，减少警告
          # 用DEBIAN_FRONTEND抑制交互提示，-y --assume-yes确认所有操作
          sudo DEBIAN_FRONTEND=noninteractive apt purge -y -qq \
            dotnet* ghc* android* firefox llvm* powershell mono-devel snapd flatpak
          sudo DEBIAN_FRONTEND=noninteractive apt autoremove -y -qq --purge
          sudo apt clean -y -qq
          sudo rm -rf /var/lib/apt/lists/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /var/lib/docker /tmp/* ~/.cache/*

          # ========== 2. 修复swapfile占用问题（核心） ==========
          # 先检查并关闭现有swap
          if swapon --show | grep -q "/swapfile"; then
            echo "检测到现有/swapfile，先关闭并删除..."
            sudo swapoff /swapfile
            sudo rm -f /swapfile
          fi
          # 再次检查是否残留（防止删除失败）
          if [ -f "/swapfile" ]; then
            echo "强制删除残留的/swapfile..."
            sudo rm -rf /swapfile
          fi

          # 创建新的8GB交换文件（改用dd命令，兼容更多场景）
          echo "创建8GB交换文件..."
          sudo dd if=/dev/zero of=/swapfile bs=1G count=8 status=progress
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          sudo sysctl vm.swappiness=10

          # ========== 3. 验证空间和swap ==========
          echo "===== 清理后磁盘/内存/swap状态 ====="
          df -h
          free -h
          swapon --show

      - name: 📥 拉取ImmortalWrt源码
        uses: actions/checkout@v4
        with:
          repository: immortalwrt/immortalwrt
          ref: ${{ env.BRANCH }}
          fetch-depth: 1

      - name: 📦 安装编译依赖（优化apt警告）
        run: |
          sudo DEBIAN_FRONTEND=noninteractive apt install -y -qq \
            build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
            gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget curl

      - name: ⚙️ 加载自定义配置
        run: |
          mv config.seed .config
          echo "CONFIG_TARGET_IMAGES_PARTITION_OVERLAY_SIZE=\"${{ env.OVERLAY_SIZE }}\"" >> .config
          make defconfig
          ./scripts/feeds update -a && ./scripts/feeds install -a
          sed -i 's/downloads.openwrt.org/mirrors.ustc.edu.cn\/openwrt/g' ./feeds.conf.default

      - name: 📥 下载编译依赖（带重试）
        run: |
          for i in {1..3}; do
            echo "===== 第 $i 次下载依赖 ====="
            make download -j$(nproc) && break
            if [ $i -eq 3 ]; then exit 1; fi
            sleep 5
          done
          find dl -size -1024c -exec rm -f {} \;

      - name: 🔨 编译固件（智能线程）
        run: |
          CORES=$(nproc)
          COMPILE_CORES=$([ $CORES -gt 4 ] && echo $((CORES-1)) || echo $CORES)
          make -j$COMPILE_CORES || make -j1 V=s
          ls -lh bin/targets/${{ env.TARGET_ARCH }}/

      - name: 📤 上传固件产物
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt-x86_64-EFI-${{ github.run_id }}
          path: |
            bin/targets/${{ env.TARGET_ARCH }}/*-efi.img.gz
            bin/targets/${{ env.TARGET_ARCH }}/*-efi.iso
            bin/targets/${{ env.TARGET_ARCH }}/*.img.gz  # 包含传统BIOS固件
          retention-days: 7

      - name: 🧹 清理临时文件
        if: always()
        run: |
          if swapon --show | grep -q "/swapfile"; then
            sudo swapoff /swapfile
            sudo rm -f /swapfile
          fi
